/**
 * @deprecated 请使用 `./prompt` 中的 `buildProfilePrompt`。
 * 保留此文件以免旧 import 路径断裂。
 */
export {
  buildProfilePrompt,
  buildProfilePrompt as profilePrompt,
  getProfileMdPath,
  loadProfileInfo,
  wrapProfileInfo,
} from './prompt'
